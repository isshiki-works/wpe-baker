# engine

The offline renderer's source tree (a modified open-wallpaper-engine, GPL-2.0) is not vendored in this repository. The exact tree and its full Git history ship in the source archive on the release page as `engine/` plus `engine-upstream.bundle`; the pinned engine commit is recorded in `SOURCE.md` and in the package build records.
