# 补丁文件要按字节做哈希核对：scripts/dependency-patches/manifest.json 里每份
# patch 的 sha256（apply-dependency-patches.py 以二进制读）。
# core.autocrlf=true 的机器上 checkout 会把 LF 变 CRLF，哈希必然对不上，
# 所以这些文件一律禁用行尾转换。
*.patch -text

# 许可文本逐字节保持上游原样：THIRD-PARTY-NOTICES.md 第 8 节列了这些文件的
# SHA256，行尾一旦被转换，用户就无法用那张表核对。
licenses-extra/** -text

# 不要在这里加 `* -text` 或给 scripts/ 下的文件加 eol=lf。
# scripts/{build-ffmpeg-lgpl21.py,build-native-cmake.py,
# distribution-inputs.lock.json,fetch-ffmpeg-lgpl21-inputs.py,
# native-inputs.lock.json,native-provenance.py} 这 6 个文件参与渲染器的逐文件
# 源码绑定（native-provenance.load_verified_build）。构建机上它们在工作树里是
# LF、native-inputs.lock.json 还是混合换行，与仓库 blob 并不逐字节相同；一旦
# 被这里的规则纳入，git status 会把它们判成 modified（打包脚本的「工作树必须
# 干净」检查随即拒绝），而按常规方式还原又会把它们重写成 CRLF、直接毁掉已经
# verified 的绑定。正确做法写在 scripts/DISTRIBUTION-DEPENDENCIES.md：切分支只用
# git fetch <bundle> <分支>:<分支> + git switch，不对 scripts/ 用 checkout -f /
# reset --hard / stash / clean -x。
# 上面两条规则只匹配 *.patch 与 licenses-extra/，
# 都不触及这 7 个文件。
